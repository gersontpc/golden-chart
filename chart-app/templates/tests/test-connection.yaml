apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-golden-apps.fullname" . }}-test-connection"
  labels:
    {{- include "helm-golden-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-golden-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
